# Nova Codex 项目指引

## 项目

- 将 Nova 视为基于 Ergo Framework v330 的 Go 分布式游戏服务端框架。
- 对于版本相关 API，以 `go.mod` 和当前检出目录或模块缓存中的 Ergo 源码为准；不得编造 Ergo 类型、字段、方法、默认值或行为保证。
- Ergo 实现和架构使用 `$ergo-framework`，游戏服务拓扑与一致性使用 `$distributed-game-server`，线上节点诊断使用 `$ergo-operations`。

## 架构规则

- 每份可变数据都必须有唯一且明确的所有者；Actor 间只传值或不可变快照，不得跨边界共享可变指针。
- Actor 回调不得无期限等待。避免阻塞 I/O、`time.Sleep`、裸 channel 接收、无限 mutex 等待，以及在回调中等待自行创建的 goroutine。
- 阻塞 socket、流、子进程及同类 I/O 应由 Ergo meta process 或带时限的外部适配层承担。
- 在节点启动前注册 EDF 类型，通常放在 `init()`；先注册嵌套类型，再注册父类型，并导出所有跨节点字段。
- 远程状态变更必须设计超时、重试、重复投递、幂等、过期消息和局部失败。Important Delivery 只确认传输，不代表业务提交成功。
- 每个故障域都要有监督边界；进程重启不能代替持久状态恢复。
- 引入 Pool、分片或额外基础设施前，先量化邮箱速率、处理耗时、状态大小、连接数和故障恢复量。

## Nova 约定

- 复用 `nova/core/log` 及其 Zap/lumberjack 基础，不得再引入第二套日志栈。
- `node.Start()` 只负责编排；每个命令行参数在对应 setter 中校验。
- 保持 `sh ctl.sh start <args...>`：`start` 之后的全部参数必须原样转发。
- 不得把密钥、生产 Cookie、Token、密码或固定生产端点写入仓库文件。

## 工作规则

- 保留用户的无关改动；工作区可能已经处于脏状态。
- 优先做小范围补丁，修改前检查相邻 handler、消息类型、supervisor 和测试。
- 变更 Go 文件后运行 `gofmt`；默认缓存不可用时使用工作区本地 `GOCACHE`。
- 先跑定向测试，之后才考虑 `go test ./...`；既有失败须单独说明。
- 严格区分证据：编译成功不是运行时证据，单测不是多节点证据，Windows 上的 `sh -n` 不是 Linux 进程/信号生命周期证据。
- 未获用户明确授权时，不得在诊断中修改运行中集群；运维调查默认只读。

## 架构设计输出

新分布式游戏功能必须说明：限界上下文、状态所有者、Actor 标识与生命周期、消息契约、持久化边界、幂等键、超时/重试行为、监督策略、路由/分片、过载策略、可观测性、发布兼容性和测试。
